[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "asml_product_p3_scheduler"
version = "0.1.0"
description = "Facility beam-split scheduler — wafers/day under shared FEL (P3)."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Proprietary" }
dependencies = []
[project.optional-dependencies]
dev = ["pytest>=7.0"]
[tool.setuptools.packages.find]
include = ["asml_product_p3_scheduler*"]
[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
