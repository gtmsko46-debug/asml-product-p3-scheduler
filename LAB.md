# Lab bind — asml-product-p3-scheduler

See [`SPEC.md`](SPEC.md).

- **Sandbox:** `labs/p3-scheduler/scheduler.py (scaffold; FEL-03 bind when ready)`
- **Eval:** product scheduler eval; bind FEL-03 when dual-KEEP exists
- **Card:** `beam-split-facility-v1`
- **Dual-gate:** dual-gate after FEL-03 feed; until then Spec/M1 package only

Live weights: `ASML_BENCH_ROOT` / product-specific override env (set at M1).
